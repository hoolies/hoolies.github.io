# Future work

## GitHub repos and Highlights section

**Reminder:** Restructure GitHub repositories to align with the skills tree (`chrysanthos/skills/`), then link them from the Highlights section on the site.

Suggested approach:

1. Organise repos by category (linux, systems, networking, automation, leadership, ai) or use a naming prefix.
2. Add new project repos as you build them.
3. In `index.html` → Highlights, add cards that map to tree entries (repo link, one-line proof, tags).
4. Optional: add `repo` field per skill in `chrysanthos/skills/skills.manifest.json` when ready.

When you are ready, ask to wire manifest repo URLs into Highlights automatically.
